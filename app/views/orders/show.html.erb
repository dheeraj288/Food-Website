<h2 class="text-2xl font-bold mb-4 text-gray-800">🧾 Order Summary - ##<%= @order.id %></h2>

<div class="bg-white p-6 rounded shadow-md border">
  <div class="mb-4">
    <p class="text-gray-600">
      Status: 
      <span class="text-indigo-700 font-semibold"><%= @order.status.capitalize %></span>
    </p>
    <p class="text-gray-600">
      Placed on: <%= @order.created_at.strftime("%d %b %Y, %I:%M %p") %>
    </p>
  </div>

  <div class="space-y-4">
    <% @order.order_items.includes(:menu_item).each do |item| %>
      <div class="flex justify-between items-center border-b pb-3">
        <div class="flex items-center gap-4">
          <% if item.menu_item&.image_url.present? %>
            <%= image_tag item.menu_item.image_url, alt: item.menu_item.name, class: "w-16 h-16 rounded object-cover" %>
          <% end %>
          <div>
            <p class="font-medium text-gray-800"><%= item.menu_item&.name || "Menu item not found" %></p>
            <p class="text-sm text-gray-500">Qty: <%= item.quantity %> × ₹<%= item.price %></p>
          </div>
        </div>
        <p class="text-indigo-700 font-bold">₹<%= item.quantity * item.price %></p>
      </div>
    <% end %>
  </div>

  <div class="mt-6 text-right">
    <h3 class="text-xl font-bold text-indigo-800">Total Paid: ₹<%= @order.total %></h3>
  </div>
</div>
