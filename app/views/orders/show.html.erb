<h2 class="text-3xl font-bold text-gray-800 mb-6">📦 Order #<%= @order.id %> Details</h2>

<div class="bg-white rounded-xl shadow p-6 border border-gray-100 space-y-4">
  <p><strong>Restaurant:</strong> <%= @order.restaurant.name %></p>
  <p><strong>Total:</strong> ₹<%= @order.total %></p>
  <p><strong>Placed on:</strong> <%= @order.created_at.strftime("%d %b %Y, %I:%M %p") %></p>

  <h4 class="font-semibold mt-4 text-lg">Items:</h4>
  <ul class="space-y-2">
    <% @order.order_items.each do |item| %>
      <li class="flex justify-between text-gray-700 border-b pb-1">
        <span><%= item.menu_item.name %> × <%= item.quantity %></span>
        <span>₹<%= item.price * item.quantity %></span>
      </li>
    <% end %>
  </ul>
</div>

<div class="mt-6">
  <%= link_to "⬅ Back to Orders", orders_path, class: "text-indigo-600 hover:underline" %>
</div>
